# Core dependencies
openai>=1.0.0
Pillow>=9.0.0

# GUI framework
# NOTE: On Raspberry Pi, PyQt6 must be installed via apt, NOT pip:
#   sudo apt install python3-pyqt6
#   Then create your venv with: python3 -m venv --system-site-packages .venv
# On macOS, uncomment the line below and install via pip normally.
# PyQt6>=6.5.0

# Audio recording (macOS)
sounddevice>=0.4.0
soundfile>=0.12.0

# Audio recording (Raspberry Pi)
pyaudio>=0.2.11

# Raspberry Pi GPIO
RPi.GPIO>=0.7.0

# AWS S3 and QR code generation (optional features)
boto3>=1.26.0
qrcode>=7.4.0
